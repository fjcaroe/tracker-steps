<?xml version="1.0" encoding="utf-8"?>
<!--
  © 2026 Steps Consulting
  License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl).
-->
<odoo>
    <record id="ir_cron_step_tracker_sync" model="ir.cron">
        <field name="name">Web Tracker: sincronizar máquinas, conductores, predios y sesiones</field>
        <field name="model_id" ref="model_step_tracker_sync"/>
        <field name="state">code</field>
        <!-- Cada compañía con la sincronización activada se sincroniza contra su propia
             instancia de Web Tracker; las demás se saltan sin generar error. -->
        <field name="code">
for company in env['res.company'].sudo().search([('step_tracker_sync_enabled', '=', True)]):
    env['step.tracker.sync'].with_company(company).run_sync(raise_on_error=False)
        </field>
        <field name="interval_number">15</field>
        <field name="interval_type">minutes</field>
        <field name="active" eval="False"/>
        <field name="user_id" ref="base.user_root"/>
    </record>
</odoo>
