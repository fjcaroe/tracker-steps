<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_step_export_estimate_tag_list" model="ir.ui.view">
        <field name="name">step.export.estimate.tag.list</field>
        <field name="model">step.export.estimate.tag</field>
        <field name="arch" type="xml">
            <list string="Estimación de Cosecha (etiqueta)">
                    <field name="name"/>
            </list>
        </field>
    </record>

    <record id="view_step_export_estimate_tag_form" model="ir.ui.view">
        <field name="name">step.export.estimate.tag.form</field>
        <field name="model">step.export.estimate.tag</field>
        <field name="arch" type="xml">
            <form string="Estimación de Cosecha (etiqueta)">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_step_export_estimate_tag" model="ir.actions.act_window">
        <field name="name">Estimación de Cosecha (etiqueta)</field>
        <field name="res_model">step.export.estimate.tag</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
