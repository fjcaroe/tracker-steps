<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_step_export_grower_discount_tag_list" model="ir.ui.view">
        <field name="name">step.export.grower.discount.tag.list</field>
        <field name="model">step.export.grower.discount.tag</field>
        <field name="arch" type="xml">
            <list string="Ítem Productor (Descuento Liquidación) (etiqueta)">
                    <field name="name"/>
            </list>
        </field>
    </record>

    <record id="view_step_export_grower_discount_tag_form" model="ir.ui.view">
        <field name="name">step.export.grower.discount.tag.form</field>
        <field name="model">step.export.grower.discount.tag</field>
        <field name="arch" type="xml">
            <form string="Ítem Productor (Descuento Liquidación) (etiqueta)">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_step_export_grower_discount_tag" model="ir.actions.act_window">
        <field name="name">Ítem Productor (Descuento Liquidación) (etiqueta)</field>
        <field name="res_model">step.export.grower.discount.tag</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
