<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_step_export_grower_rate_tag_list" model="ir.ui.view">
        <field name="name">step.export.grower.rate.tag.list</field>
        <field name="model">step.export.grower.rate.tag</field>
        <field name="arch" type="xml">
            <list string="Tarifa Productor (etiqueta)">
                    <field name="name"/>
            </list>
        </field>
    </record>

    <record id="view_step_export_grower_rate_tag_form" model="ir.ui.view">
        <field name="name">step.export.grower.rate.tag.form</field>
        <field name="model">step.export.grower.rate.tag</field>
        <field name="arch" type="xml">
            <form string="Tarifa Productor (etiqueta)">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_step_export_grower_rate_tag" model="ir.actions.act_window">
        <field name="name">Tarifa Productor (etiqueta)</field>
        <field name="res_model">step.export.grower.rate.tag</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
