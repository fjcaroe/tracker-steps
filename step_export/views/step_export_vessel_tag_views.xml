<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_step_export_vessel_tag_list" model="ir.ui.view">
        <field name="name">step.export.vessel.tag.list</field>
        <field name="model">step.export.vessel.tag</field>
        <field name="arch" type="xml">
            <list string="Nave (etiqueta)">
                    <field name="name"/>
            </list>
        </field>
    </record>

    <record id="view_step_export_vessel_tag_form" model="ir.ui.view">
        <field name="name">step.export.vessel.tag.form</field>
        <field name="model">step.export.vessel.tag</field>
        <field name="arch" type="xml">
            <form string="Nave (etiqueta)">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_step_export_vessel_tag" model="ir.actions.act_window">
        <field name="name">Nave (etiqueta)</field>
        <field name="res_model">step.export.vessel.tag</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
