<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_step_export_shipping_instruction_tag_list" model="ir.ui.view">
        <field name="name">step.export.shipping.instruction.tag.list</field>
        <field name="model">step.export.shipping.instruction.tag</field>
        <field name="arch" type="xml">
            <list string="Instructivo de Embarque (etiqueta)">
                    <field name="name"/>
            </list>
        </field>
    </record>

    <record id="view_step_export_shipping_instruction_tag_form" model="ir.ui.view">
        <field name="name">step.export.shipping.instruction.tag.form</field>
        <field name="model">step.export.shipping.instruction.tag</field>
        <field name="arch" type="xml">
            <form string="Instructivo de Embarque (etiqueta)">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_step_export_shipping_instruction_tag" model="ir.actions.act_window">
        <field name="name">Instructivo de Embarque (etiqueta)</field>
        <field name="res_model">step.export.shipping.instruction.tag</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
