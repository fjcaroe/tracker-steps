<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_step_packing_activity_tag_list" model="ir.ui.view">
        <field name="name">step.packing.activity.tag.list</field>
        <field name="model">step.packing.activity.tag</field>
        <field name="arch" type="xml">
            <list string="Actividad Agrícola (etiqueta)">
                    <field name="name"/>
            </list>
        </field>
    </record>

    <record id="view_step_packing_activity_tag_form" model="ir.ui.view">
        <field name="name">step.packing.activity.tag.form</field>
        <field name="model">step.packing.activity.tag</field>
        <field name="arch" type="xml">
            <form string="Actividad Agrícola (etiqueta)">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_step_packing_activity_tag" model="ir.actions.act_window">
        <field name="name">Actividad Agrícola (etiqueta)</field>
        <field name="res_model">step.packing.activity.tag</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
