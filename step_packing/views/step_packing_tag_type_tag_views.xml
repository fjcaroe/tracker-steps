<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_step_packing_tag_type_tag_list" model="ir.ui.view">
        <field name="name">step.packing.tag.type.tag.list</field>
        <field name="model">step.packing.tag.type.tag</field>
        <field name="arch" type="xml">
            <list string="Tipo de Tarja (etiqueta)">
                    <field name="name"/>
            </list>
        </field>
    </record>

    <record id="view_step_packing_tag_type_tag_form" model="ir.ui.view">
        <field name="name">step.packing.tag.type.tag.form</field>
        <field name="model">step.packing.tag.type.tag</field>
        <field name="arch" type="xml">
            <form string="Tipo de Tarja (etiqueta)">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_step_packing_tag_type_tag" model="ir.actions.act_window">
        <field name="name">Tipo de Tarja (etiqueta)</field>
        <field name="res_model">step.packing.tag.type.tag</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
