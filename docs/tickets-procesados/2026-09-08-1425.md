# Tickets procesados — 2026-09-08 14:25 (hora local)

Corrida automática `correos-fcaro-gestion-costos`.

## Resumen

- Tickets asignados abiertos: **8** (#9, #11, #12, #13, #14, #15, #16, #17).
- Ya procesados en corridas anteriores (tienen nota `⟦IA-EVIDENCIA⟧` vigente,
  sin comentarios nuevos del cliente): **#9, #11, #12, #13, #14, #15, #16** →
  **omitidos**.
- Procesado en esta corrida: **#17**.
- **Incidencia (resuelta):** durante la corrida, `helpdesk_api.json` quedó
  como JSON inválido (dos objetos concatenados) y falló toda llamada al
  Helpdesk. El usuario lo separó en dos archivos
  (`helpdesk_api.json` + `demo-sys_api.json`) y **se retomó**: la nota
  `⟦IA-EVIDENCIA⟧` (msg 8416) y el adjunto (`ir.attachment` 2866) **quedaron
  publicados en el ticket #17**. Etapa sin cambiar: no existe etapa de
  revisión/validación (las etapas son New / In Progress / On Hold / Solved /
  Cancelled). Ticket **no cerrado**.

---

## #17 — Somed, Carolina Medel, error imposiciones con licencia médica

- **Área:** Nómina / PreviRed / imposiciones.
- **Worktree:** `C:\Users\tito4\Documents\Odoo` · **Rama base:** `fix/previred-correcciones-2`.
- **Rama creada:** `ticket/17-somed-medel-licencia-medica` · commit `6d9860c`.
- **Subida a GitHub:** ✅ `origin/ticket/17-somed-medel-licencia-medica`
  (repo `github.com/fjcaroe/tracker-steps`).
- **Diffstat** (`fix/previred-correcciones-2..ticket/17-somed-medel-licencia-medica`):

  ```
   docs/HELPDESK_T17_SOMED_MEDEL_LICENCIA_MEDICA_2026-09.md | 174 +++++++++++++++++++++
   1 file changed, 174 insertions(+)
  ```

- **Qué se hizo:** SOLO un documento de análisis. **Ningún cambio de código,
  ninguna prueba agregada.** Es un cálculo que mueve montos declarados a
  PreviRed y parte del cálculo vive en el motor de nómina del proveedor en
  `:8070`, fuera de estos repos → por regla, no se implementa a ciegas;
  requiere confirmación funcional.

- **Diagnóstico:** otro caso del "corte 2" de licencia médica (ver
  `docs/PREVIRED_TICKET_LICENCIA_JORNADA_2026-09.md` en la rama
  `correo/20260907-previred-licencia-jornada`). Carolina Medel Gutiérrez,
  RUT 17.932.663-9, agosto 2026, mes completo de licencia (campo 13 = 0,
  licencia continua 20-07 → 31-08). El archivo de revisión que adjunta el
  cliente aporta el dato que faltaba en el corte 2: las líneas PreviRed ya
  generadas por el ERP para un mes completo de licencia.

  Discrepancias (ERP → correcto), base de aportes patronales = 1.205.761:

  | Campo | Concepto | ERP | Correcto |
  |------:|----------|----:|---------:|
  | 29 | SIS | 22.178 | 21.463 |
  | 92 | RIMA | 0 | 81.148 |
  | 94 | CEV | 8.681 | 8.681 (OK) |
  | 95 | Rentabilidad Protegida | 0 | 10.852 |
  | 102 | AFC empleador | 29.903 | 28.938 |
  | 71 | Acc. Trabajo ISL | — | 11.214 (según cliente) |

  - **Campo 95:** defecto local del exportador —
    `step_hr_previred/models/previred_extractor.py::_enrich_official_fields`
    calcula el campo 95 sobre el campo 27 (Renta Imponible AFP), que es 0 en
    licencia de mes completo. La tasa (0,90 %) ya es correcta. El campo 94 se
    salva por la precedencia de la línea de nómina; el 95 no la tiene.
  - **Campos 29 y 102:** base inflada ≈ +40.196 por el motor de nómina
    (`:8070`); mismo patrón que el ticket #15 (Mutual). No se corrige desde
    el exportador sin acceso a `:8070` o un override en
    `step_hr_previred_simpledigital`.
  - **Campo 92 (RIMA):** pendiente §2 del corte 2; este ticket confirma que
    hace falta.

- **Contradice el supuesto del corte 2:** el documento asume "imponible del
  mes ≈ 0 cuando días = 0"; este caso muestra base = 1.205.761.

- **Pendiente / requiere confirmación funcional:**
  1. Qué es exactamente 1.205.761 (¿sueldo base + gratificación, o imponible
     del mes + RIMA?).
  2. ¿Misma base para SIS, CEV, RENT_PROT y AFC empleador?
  3. ¿1.205.761 y RIMA 81.148 son independientes o sumados?
  4. Somed usa ISL (campo 71): con 43 días continuos, ¿campo 71 = 11.214 o 0?
  5. El +40.196 del motor: ¿regla conocida o bug?

- **NO DESPLEGADO.** Suite no ejecutada. Etapa del ticket sin cambiar
  (la automatización no puede enumerar `helpdesk.ticket.stage`).

- **Nota interna + adjunto:** **publicados** (tras corregir `helpdesk_api.json`).
  Nota `⟦IA-EVIDENCIA⟧` = `mail.message` 8416; adjunto
  `HELPDESK_T17_SOMED_MEDEL_LICENCIA_MEDICA_2026-09.md` = `ir.attachment` 2866.

---

## Incidencia con las credenciales

Mensaje del usuario a mitad de corrida: *"cree la key para demo-sys
C:\Users\tito4\.odoo\helpdesk_api.json"*.

Tras ese mensaje, `helpdesk_api.json` contiene **dos objetos JSON
concatenados** (12 líneas: `{...},` seguido de `{...}`), lo que **no es JSON
válido**. El cliente `odoo_helpdesk.py` hace `json.load()` de un único objeto
con claves `url/db/username/api_key`, así que **toda llamada al Helpdesk falla**
con `JSONDecodeError: Extra data: line 6`.

- **No se editó el archivo** — está prohibido por el instructivo de la tarea
  ("Editar `.env`, secretos, `helpdesk_api.json`").
- **No se conectó a demo-sys / `:8070`** — tocar ese ambiente también está
  prohibido para esta automatización, y el instructivo dice que, ante fallo
  de autenticación, hay que avisar y terminar.

### Qué falta para que la próxima corrida publique la nota de #17

Corregir `C:\Users\tito4\.odoo\helpdesk_api.json` para que sea **un solo
objeto JSON** (el del Helpdesk `:8069`). Si se quiere guardar también la
credencial de demo-sys, hacerlo en **otro archivo** (p. ej.
`C:\Users\tito4\.odoo\demo_sys_api.json`), no dentro de éste. Estructura
válida esperada:

```json
{
  "url": "http://35.222.25.110:8069",
  "db": "<base_helpdesk>",
  "username": "<usuario>",
  "api_key": "<api_key>"
}
```

Con eso arreglado, publicar en el ticket #17: la nota `⟦IA-EVIDENCIA⟧`
(`scratchpad/note17.txt`, o rehacerla) y adjuntar
`HELPDESK_T17_SOMED_MEDEL_LICENCIA_MEDICA_2026-09.md`.

---

## Respaldo GitHub (Paso 4)

`fetch` + revisión en los 3 worktrees (`Odoo`, `Odoo-gestion-costos`,
`Odoo-steps-task` — todos worktrees del mismo repo).

- **Ramas subidas en esta corrida:** `ticket/17-somed-medel-licencia-medica`
  → `origin/ticket/17-somed-medel-licencia-medica`.
- **Ramas locales:** todas en sync con `origin` (ahead=0, behind=0). Ninguna
  divergida. Nada pendiente de subir.
- **Ramas divergidas no subidas:** ninguna.
- **Trabajo sin commitear detectado (preexistente, NO tocado):**
  - `C:\Users\tito4\Documents\Odoo`: ~129 archivos sin commitear
    (preexistente; incluye `step_account_treasury/*`, `docs/PREVIRED_MATRIZ.md`,
    etc.). Más el reporte nuevo `docs/tickets-procesados/2026-09-08-1425.md`.
  - `C:\Users\tito4\Documents\Odoo-gestion-costos`: ~54 archivos sin commitear
    (preexistente; `step_management_costs/*` — coincide con el snapshot de
    inicio de sesión).
  - `C:\Users\tito4\Documents\Odoo-steps-task`: limpio.
- **Worktrees temporales:** ninguno (`git worktree list` en `Odoo` muestra
  solo los 3 permanentes; el temporal `wt-t17` se removió).

---

## Revisión del usuario

```
git -C "C:\Users\tito4\Documents\Odoo" log --oneline fix/previred-correcciones-2..ticket/17-somed-medel-licencia-medica
git -C "C:\Users\tito4\Documents\Odoo" diff fix/previred-correcciones-2..ticket/17-somed-medel-licencia-medica
```

Fernando decide si integra el análisis al corte 2, responde las preguntas
funcionales de la sección "Pendiente" y publica la nota en el ticket #17.
